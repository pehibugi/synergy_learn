def question_1():
    """Цель: С помощью условий определить какое число ввел пользователь:
    1. Отрицательное четное
    2. Нулевое
    3. Отрицательное нечетное
    4. Положительное четное
    5. Положительное нечетное"""

    # Ввод числа
    answer = int(input('Введите число:'
                       '\n>>> '))

    # Если число равно 0, то...
    if answer == 0:
        print('Пользователь ввел число 0')

    # Если число равно четному положительному, то ...
    elif answer % 2 == 0 and answer > 0:
        print('Пользователь ввел четное положительное число.')

    # Если число равно нечетному положительному, то ...
    elif answer % 2 != 0 and answer > 0:
        print('Пользователь ввел нечетное положительное число')

    # Если число равно четному отрицательному, то ...
    elif answer < 0 and answer % 2 == 0:
        print('Пользователь ввел отрицательное четное число')

    # Если число равно нечетному отрицательному, то ...
    elif answer % 2 != 0 and answer < 0:
        print('Пользователь ввел нечетное отрицательное число.')

    else:
        print('Число не распознано!')

def question_2():
    '''Цель: Посчитать сколько гласных и согласных букв в введенном с помощью input() слове\предложении.'''

    # Список гласных
    vowel_list = ['а', 'о', 'у', 'ы', 'э', 'е', 'ё', 'и', 'ю', 'я']

    # Список согласных
    consonant_list = ['в', 'б', 'г', 'д', 'ж', 'к', 'л', 'м', 'н', 'п', 'р', 'с', 'т', 'ф', 'x', 'ц', 'ч', 'ш', 'щ']

    # Данные пользователя
    word = input('Введите слово\предложение\n>>> ').lower()
    word = list(map(str, str(word)))

    # поиск гласных
    glass = 0
    for i in word:
        for k in vowel_list:
            # print(f'Сравнение {i} и {k}')

            if i == k:
                # print('успех')
                glass += 1

            elif i != k:
                # print('провал')
                pass

            else:
                pass

            # print('-' * 15 + '\n')

    # поиск согласных
    soglass = 0
    for i in word:
        for k in consonant_list:
            # print(f'Сравнение {i} и {k}')

            if i == k:
                # print('успех')
                soglass += 1
                # print('+1')

            elif i != k:
                # print('провал')
                pass

            else:
                pass

            # print('-' * 15 + '\n')

    print(f'Результат:\n'
          f'- Количество гласных: {glass}\n'
          f'- Количество согласных: {soglass}')

question_1()
# question_2()
