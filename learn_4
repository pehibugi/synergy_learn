# Вывод целых чисел в которых есть 0
def question_1(quest, num_quest):
    '''Задание:
    1. Ввод числа (N)
    2. Вывести несколько чисел. Количество чисел(целых) равно введенному числу (N)
    3. Подсчитать сколько выведенных чисел равны 0 и выведите это число'''

    print(quest.format(num_quest))
    # Ввод числа
    N = int(input('Введите число\n>>> '))

    # Вывести столько цифр, сколько указано в переменной N
    x = []
    for i in range(0, N + 1):
        print(i)
        x.append(i)

    # Сколько нолей было найдено
    coin = 0
    for i in x:
        if i % 10 == 0:
            coin += 1

    else:
        pass

    print(f'Количество использованных нолей: {coin}')
    return

# Подсчет натуральных делителей числа
def question_2(quest, num_quest):
    '''
    1. Ввести натуральное число(X)
    2. Подсчитать колличество натуральных делителей числа(от 1 до X)
    '''

    print(quest.format(num_quest))
    # Ввод числа (X)
    X = int(input('Введите число:\n>>> '))

    # Подсчет количества натуральных делителей
    num = 0 # Сколько натуральных делителей
    coin = X # Счетчик
    while coin != 0:
        # Если делится без остатка, то добавить +1
        if coin % 2 == 0:
            num += 1

        # В остальных случаях ничего не делать
        else:
            pass

        coin -= 1

    print(f'Итог:\n'
          f'- Введенное число: {X}\n'
          f'- Количество натуральных делителей: {num}')
    return

# Вывод четных чисел
def question_3(quest, num_quest):
    '''Задание:
    1. Ввод двух чисел (A и B)
    2. A не может быть больше B
    3. Вывод всех четных чисел через пробел'''

    print(quest.format(num_quest))
    # Первое число (A)
    A = int(input('Введите первое число:\n>>> '))

    # Второе число (B)
    B = int(input('Введите второе число:\n>>> '))

    # Условие проверяющее, что Первое число(A) меньше второго(B)
    if A < B:
        pass
    else:
        print('По условиям первое число не может быть больше второго!')
        exit(0)

    # Вывод четных чисел через пробел
    x = ''
    for i in range(A, B + 1):
        if i % 2 == 0:
            x += str(f'{i} ')

    print(f'Список четных чисел: {x}')
    return

quest = 'Задание №{}'
question_1(quest, 1)
question_2(quest, 2)
question_3(quest, 3)
